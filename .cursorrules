# Spec 驱动开发工作流 — Cursor 入口

本项目是一个 **Spec 规格文件驱动的开发流程工具**，专为 Cursor IDE 设计。

## 核心理念

- **Spec 是唯一真相源**：所有设计决策、验收标准、审查结果都写入 `specs/` 目录
- **Human Gate 强制确认**：3 个关键节点必须停下来等你确认（需求 → 方案 → Phase 完成）
- **反狡辩体系**：implementer/reviewer/verifier 各有反狡辩表，防止 AI 用借口跳过关键步骤
- **三层约束**：Rules（静态）+ Hooks（动态程序化）+ Subagents（独立执行）

## 快速开始

```
/feature 实现用户登录功能
```

然后按照 Human Gate 提示确认每一步即可。

## 规则文件

| 文件 | 类型 | 内容 |
|------|------|------|
| `.cursor/rules/spec-workflow.mdc` | Always Apply | 核心约束 + 3 个 Human Gate + 反狡辩准则 + Pipeline DAG + 压缩恢复 |

## 子Agent（6 个）

| Agent | 职责 | 反狡辩表 |
|-------|------|:--:|
| `requirement-analyst` | 需求分析 → requirements.md | - |
| `plan-generator` | 架构设计 + Phase 拆分 → design.md + phase-plan.md | - |
| `implementer` | 按 spec 实现代码 | ✅ 7 条 |
| `reviewer` | 三视角审查 + 桩检测 | ✅ 5 条 |
| `verifier` | 独立端到端验证 | ✅ 6 条 |
| `code-explorer` | 只读代码探索 | - |

## 工作流（6 阶段 + 3 Human Gate）

```
/feature → requirement-analyst → 🛑 HG-1 需求确认 → plan-generator → 🛑 HG-2 方案确认
         → implementer → reviewer → verifier → 🛑 HG-3 Phase 完成
         → (下一 Phase 循环)
```

## 目录结构

```
.cursor/
├── rules/spec-workflow.mdc    # 核心约束
├── agents/                    # 6 个子Agent
├── hooks/                     # 3 个 hook 脚本
├── hooks.json                 # Hook 注册
└── commands/                  # /feature + /bugfix

specs/                         # 所有 spec 文件
├── current-status.md          # 工作流状态（含 HG 状态追踪）
├── requirements.md
├── design.md
├── phase-plan.md
└── phases/<phase-id>/
    ├── spec.md
    ├── implementation.md
    ├── review.md
    └── verification.md
```
