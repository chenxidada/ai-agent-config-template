# Requirements Output Template

<!--
  此模板由 requirement-analyst 使用。
  为所有下游工作（plan-generator, implementer, reviewer, verifier）提供可量化的需求基础。
  
  质量要求：plan-generator 读完此文档后，无需向 Cursor Agent 追问即可开始架构设计。
-->

## 产品目标

<!-- 一句话：这个产品/功能想要达成什么 -->

## 问题陈述

<!-- 解决什么问题？谁有这个问题？ -->

## 目标终态

<!-- 完全实现后，成功长什么样？ -->

## 目标用户

<!-- 主要用户类型及其关键特征 -->

## 核心场景

<!-- 关键用户场景（用户故事或用例），编号便于引用 -->

## 预期范围

<!-- 完整范围边界。明确什么在范围内、什么不在。不要人为缩减为 MVP —— 捕获用户的完整意图 -->

## 功能区域

<!-- 将需求按功能区域分组。每个区域列出具体需求 -->

## 验收标准（EARS 格式）

<!--
  每条 AC 必须使用以下 5 种 EARS 模式之一：
  
  | 模式 | 语法 | 示例 |
  |------|------|------|
  | 普遍型 | <系统> 必须 <响应> | 系统必须在登录失败 3 次后锁定账户 |
  | 事件驱动型 | 当 <触发> 时，<系统> 必须 <响应> | 当用户点击提交时，系统必须在 3s 内显示反馈 |
  | 状态驱动型 | 在 <状态> 期间，<系统> 必须 <响应> | 在会话有效期间，系统必须验证 JWT |
  | 不期望型 | 如果 <条件>，那么 <系统> 必须 <响应> | 如果金额超过余额，那么系统必须阻止转账 |
  | 可选型 | 若 <功能已启用>，<系统> 必须 <响应> | 若启用了 2FA，系统必须要求验证码 |

  每条 AC 以 AC-N: 开头，标注模式类型。
  使用 bold 标记关键字（当/在/如果/那么/必须）。
  每条 AC 可独立测试 —— 读到的人能明确判断 ✅ 或 ❌。
-->

## 不在范围内（明确排除）

<!-- 只有用户确认不需求的项，或与目标明确无关的项 -->

## 约束

<!-- 技术、业务或时间约束 -->

## 开放问题

<!-- 需要在继续前由用户确认的问题。编号 Q-1, Q-2... 方便引用 -->

## 风险/假设

<!-- 已知风险和设计将基于的假设 -->

## 建议的 Phase 拆分方向

<!-- 高层指引：plan-generator 应该按什么逻辑拆分 Phase -->
